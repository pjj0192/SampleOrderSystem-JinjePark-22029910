# PRD.md — 반도체 시료 생산주문관리 시스템 (SampleOrderSystem)

이 문서는 이 저장소 하나만으로 전체 요구사항을 이해할 수 있도록 작성된 제품 요구사항
문서(Product Requirements Document)다. 구현 계획은 [Plan.md](./Plan.md), 작업
가이드(Harness)는 [CLAUDE.md](./CLAUDE.md)를 참고한다.

## 1. 배경

가상의 반도체 회사 **S-Semi**는 다양한 반도체 시료(Sample)를 생산하여 연구소,
팹리스(Fabless) 업체, 대학 연구실 등에 납품한다. 시료는 주문이 들어오면 웨이퍼 공정
설비를 통해 제작되고, 검수를 거쳐 고객에게 출고된다.

주문량이 급증하면서 "이 주문 처리됐나요?", "언제 완성되는지 모르겠어요", "재고가
있는데 왜 추가 공정이 도나요?" 같은 문제가 발생했다. 엑셀·메모장 기반 관리로는 실수가
잦고 재고·공정 현황을 한눈에 파악하기 어려웠다. 이를 해결하기 위한 **콘솔 기반**
"반도체 시료 생산주문관리 시스템"이 이 프로젝트의 목표다.

## 2. 역할과 흐름

| 역할 | 설명 |
|---|---|
| 고객 (시료 요청자) | 필요한 시료를 이메일 등으로 요청 |
| 주문 담당자 | 요청에 맞게 주문서(Order)를 작성/접수 |
| 생산 담당자 | 주문 승인/거절, 시료 생산 관리 |

```
고객 --[시료 요청]--> 주문 담당자 --[주문서 전달]--> 생산 담당자
                                    <--[승인 / 거절]--
```

이 콘솔 앱은 위 세 역할의 작업을 사용자가 메뉴를 통해 직접 수행하는 단일 프로그램이다
(역할별 로그인/권한 분리는 범위 밖).

## 3. 시스템 개요

- **생산 라인**: 시료 하나를 생산하는 설비 흐름. 단일 라인이며 시료를 하나씩 순차
  생산한다. **주문이 들어온 시료에 대해서만** 생산한다 (재고 확보용 선제 생산 없음).
- **인터페이스**: 콘솔(터미널) 기반. 담당자가 메뉴 번호를 입력해 조작한다.
- **데이터 저장**: JSON 파일. 애플리케이션을 재시작해도 데이터가 유지된다.

## 4. 데이터 모델

### Sample (시료)

| 필드 | 타입 | 설명 |
|---|---|---|
| `sample_id` | str | 고유 ID (예: `S-001`) |
| `name` | str | 시료명 |
| `avg_production_time` | float | 평균 생산시간 (분/개) |
| `yield_rate` | float | 수율 `(0, 1]`. 정상품 비율이 아니라, 부족분을 정확히 채우기 위해 얼마나 더 생산해야 하는지를 역산하는 계수(§7.5 참고) |
| `stock` | int | 현재 재고 수량 (등록 시 0) |

### Order (주문)

| 필드 | 타입 | 설명 |
|---|---|---|
| `order_id` | str | 주문번호 (예: `ORD-20260416-0043`) |
| `sample_id` | str | 대상 시료 ID |
| `customer_name` | str | 고객명 |
| `quantity` | int | 주문 수량 |
| `status` | Enum | `RESERVED`/`REJECTED`/`PRODUCING`/`CONFIRMED`/`RELEASED` |
| `created_at` / `updated_at` | datetime | 생성/변경 시각 |

### ProductionJob (생산 작업)

`PRODUCING` 상태 주문이 생산 큐(FIFO)에서 처리되는 단위.

| 필드 | 타입 | 설명 |
|---|---|---|
| `job_id` | str | 생산 작업 ID |
| `order_id` / `sample_id` | str | 연결된 주문/시료 |
| `shortage` | int | 부족분 = `order.quantity - sample.stock` |
| `actual_quantity` | int | 실 생산량 = `ceil(shortage / yield_rate)` |
| `total_time` | float | 총 생산 시간 = `avg_production_time * actual_quantity` |
| `progress` | float | 진행률 0.0~1.0 |
| `enqueued_at` | datetime | 큐 등록 시각 (FIFO 정렬 기준) |

관계: `Sample (1) --< Order (N)`, `Order (1) --< ProductionJob (0..1)` (PRODUCING일 때만).

## 5. 주문 상태 흐름

| 상태 | 의미 |
|---|---|
| `RESERVED` | 주문 접수 |
| `REJECTED` | 주문 거절 (정상 흐름 외, 모니터링 제외) |
| `PRODUCING` | 승인 완료 + 재고 부족으로 생산 중 |
| `CONFIRMED` | 승인 완료 + 출고 대기 중 |
| `RELEASED` | 출고 완료 |

### 전이 규칙

| From | To | 트리거 | 부가 효과 |
|---|---|---|---|
| — | `RESERVED` | 고객 주문 접수 | 주문 레코드 생성 |
| `RESERVED` | `REJECTED` | 거절 처리 | 없음 |
| `RESERVED` | `CONFIRMED` | 승인 + 재고 충분 | 재고에서 주문 수량 즉시 차감 |
| `RESERVED` | `PRODUCING` | 승인 + 재고 부족 | 생산 큐에 Job 등록 |
| `PRODUCING` | `CONFIRMED` | 생산 완료 | 아래 §7.5 "생산 완료 후 재고 처리" 참고 |
| `CONFIRMED` | `RELEASED` | 출고 처리 | 재고는 이미 차감됨 — 출고 이력만 기록 |

**재고 차감 시점**: 승인(`CONFIRMED` 전환) 시 즉시 차감. 재고 충분/부족 두 경로 모두
동일 규칙이 적용된다 (부족 경로는 생산 완료로 재고가 채워진 직후 적용).

## 6. 메인 메뉴

| 메뉴 | 의미 |
|---|---|
| 시료 관리 | 신규 시료 등록, 목록 조회, 이름 검색 |
| 시료 주문 | 고객 주문 접수(예약) |
| 주문 승인/거절 | 생산 담당자의 승인·거절 처리 |
| 모니터링 | 상태별 주문 수 및 시료별 재고 현황 |
| 생산 라인 조회 | 현재 생산 중인 시료 및 대기 큐 확인 |
| 출고 처리 | `CONFIRMED` 상태 주문에 대해 출고 실행 |
| 종료 | 애플리케이션 종료 |

앱 시작 시 전체 요약(등록 시료 수, 총 재고, 전체 주문 수, 생산라인 대기 건수)을 함께
표시한다.

## 7. 기능 상세

### 7.1 시료 관리

- **등록**: 시료 ID/이름/평균 생산시간/수율 입력. 재고는 0으로 시작. 검증: ID 중복
  불가, 생산시간 > 0, `0 < 수율 <= 1`.
- **조회**: 등록된 모든 시료 + 현재 재고 표시.
- **검색**: 이름 부분 일치(대소문자 무관).

### 7.2 시료 주문 (예약)

- 입력: 시료 ID, 고객명, 주문 수량.
- 결과: `Order` 생성, 상태 `RESERVED`. 이 단계에서는 재고 확인/생산 배정을 하지 않는다.

### 7.3 주문 승인/거절

- `RESERVED` 목록 표시 → 특정 주문 승인/거절.
- **승인**: `sample.stock >= order.quantity`면 즉시 `CONFIRMED` (재고 차감).
  아니면 부족분만큼 생산 큐에 등록하고 `PRODUCING`.
- **거절**: 즉시 `REJECTED`.

### 7.4 모니터링

- **주문량 확인**: 상태별(`RESERVED`/`CONFIRMED`/`PRODUCING`/`RELEASED`) 건수.
  `REJECTED`는 무효 주문이므로 집계 제외.
- **재고량 확인**: 시료별 재고 + 상태.
  - 미처리 수요(pending_demand) = 해당 시료의 `RESERVED`+`PRODUCING` 주문 수량 합계
  - 잔여율(ratio) = `stock / max(pending_demand, 1)`
  - 판정: `stock == 0` → **고갈** (최우선) / `ratio >= 0.5` → **여유** / 그 외 → **부족**

### 7.5 생산 라인

- **계산식**
  - 부족분 = `order.quantity - sample.stock`
  - 실 생산량 = `ceil(부족분 / yield_rate)`
  - 총 생산 시간 = `avg_production_time * 실 생산량`
- **생산 완료 후 재고 처리 (중요)**: 실 생산량은 전량 양품으로 간주해 재고에 먼저
  입고한 뒤, 주문 수량만큼만 차감해 `CONFIRMED`로 전환한다. 나머지(`실생산량 - 부족분`)
  는 잉여 재고로 남아 이후 다른 주문에 재사용된다.
  - 예: 주문 16, 현재 재고 0, 수율 0.8 → 부족분 16 → 실 생산량 `ceil(16/0.8)=20`.
    생산 완료 시 재고에 20 입고(`stock=20`) → 주문 수량 16 차감(`stock=4`) →
    `CONFIRMED`. 남은 4는 일반 재고.
  - 수율은 "생산 중 불량이 나서 폐기된다"는 뜻이 아니라, 부족분을 정확히 채우는 데
    필요한 생산량을 역산하는 계수일 뿐이다.
- **생산 큐**: 단일 생산 라인, FIFO(선입선출).
- **조회 기능**: 현재 생산 중인 작업(주문 정보, 진행률), 대기 큐(FIFO 순서).

### 7.6 출고 처리

- `CONFIRMED` 목록에서 특정 주문 선택 → 출고 실행 → `RELEASED` 전환.
- 재고는 승인 시점에 이미 차감되었으므로, 출고 시에는 추가 차감 없이 이력만 기록.

## 8. 확정된 기술/설계 결정

| 항목 | 결정 |
|---|---|
| 언어/스택 | Python 3.11+, 콘솔(표준 라이브러리만) |
| 아키텍처 | MVC (Model/Controller/View/Service/Repository 계층 분리) |
| 데이터 영속성 | JSON 파일, 원자적 쓰기(temp + `os.replace`) |
| 생산 라인 개수 | 단일 라인 |
| 오차 반영 | 수율만 반영, 별도 오차 계수 없음 |
| 재고 차감 시점 | 승인(`CONFIRMED`) 전환 시 |
| 생산 완료 후 재고 | 전량 입고 → 주문 수량 차감 → 잉여분 재고 잔존 |
| 모니터링 판정 기준 | 잔여율 50% 경계, `stock==0` 최우선 고갈 |
| 출고 완료 상태값 | `RELEASED` |

## 9. 4개 PoC 저장소와의 관계

이 저장소는 아래 4개 PoC 저장소에서 개별 검증된 요소를 통합·확장한다
(재사용 전략은 [Plan.md](./Plan.md) 참고).

| PoC 저장소 | 재사용 요소 |
|---|---|
| `ConsoleMVC-JinjePark-22029910` | Model/Controller/View 계층 분리 패턴, Fake View를 이용한 Controller 테스트 방식 |
| `DataPersistence-JinjePark-22029910` | JSON 파일 Repository 패턴 (원자적 쓰기, 재실행 시 데이터 유지) — Order/ProductionJob에도 동일 패턴 확장 |
| `DummyDataGenerator-JinjePark-22029910` | 시드 가능한 RNG 기반 더미 데이터 생성 패턴 (관리자용 초기 데이터 채우기에 활용 가능) |
| `DataMonitor-JinjePark-22029910` | 모니터링 집계 로직(`order_status_summary`/`stock_summary`)과 판정 기준 |
